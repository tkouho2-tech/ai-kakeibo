# Kakeibo_Final_v3 (マイニー) システム仕様書

## 1. システム概要
「マイニー（Ver 4.4.9）」は、Streamlitをフロントエンド、Google Sheetsをデータベースとして採用した高機能AI家計簿アプリケーションです。Gemini APIを活用した「レシート画像の自動解析」や「専属FPとしてのAI相談機能」を搭載し、ユーザーのライフスタイルに合わせたパーソナライズされた家計管理体験を提供します。

## 2. 技術スタック・利用ライブラリ
* **フロントエンド / バックエンド**: Python, Streamlit
* **データベース**: Google Sheets (`gspread`, `google.oauth2.service_account`)
* **データ処理・可視化**: `pandas`, `plotly.express`, `plotly.graph_objects`
* **AI・LLM機能**: `google-genai` (Gemini 2.5 Flash)
* **認証**: `bcrypt`
* **画像処理**: `Pillow (PIL)`, `base64`
* **その他ユーティリティ**: `jpholiday`, `xlsxwriter`, `dateutil`

## 3. データモデル (Google Sheets構成)
スプレッドシート名: `Kakeibo_Data`

### 3.1. `users` (ユーザー認証用シート)
* `username`: ユーザーID（小文字化して保存）
* `password_hash`: bcryptでハッシュ化されたパスワード

### 3.2. `transactions` (トランザクション/家計簿データ)
* `username`, `date`, `store_name`, `item_name`, `category` (大分類), `subcategory` (小分類), `amount`, `update` (更新日時), `payment_method`, `payment_type`, `closing_date`, `payment_month`, `payment_date`

### 3.3. `User_Master` (ユーザープロフィールシート)
* `username`, `name` (氏名), `gender` (性別), `birthdate` (生年月日), `mbti` (MBTI), `occupation` (職業), `hobbies` (趣味), `life_stance` (ライフスタンス), `ai_base_instruction` (AI相談の基本指示)

### 3.4. `Payment_Master` (支払方法マスターシート)
* `username`, `payment_id`, `name`, `type`, `closing_date`, `payment_month`, `payment_date`, `is_credit_card`, `credit_limit`

## 4. 機能仕様・画面構成

### 4.1. 認証機能
* **ログイン・新規登録**: bcryptによるハッシュ化パスワード認証。新規登録時はデフォルトの支払い方法（未設定、現金、PayPay）が自動登録される。

### 4.2. 表示・分析系
* **ダッシュボード（月次集計）**:
  * 月間の総支出、予算状況、日別支出推移グラフの表示。
  * 「店舗別」「大分類別」「小分類別」での内訳・ドリルダウン分析。
* **ダッシュボード（年次集計）**:
  * 前年対比棒グラフ、年次大分類別シェアの円グラフ表示。
* **カレンダー**:
  * 月間カレンダー形式で日々の支出を俯瞰。日付選択で詳細表示。土日祝日は色分け。
* **クレジットカード**:
  * 「当月支払」「次回支払額」「次回以降支払額」の3期間に分けた利用額表示。
  * 5階層（日付 ＞ 店舗 ＞ 大分類 ＞ 小分類 ＞ 商品名）でのドリルダウン。
  * 各カードの利用限度額とプログレス表示。

### 4.3. レシート管理 (入力・修正機能)
* **レシート取込 (AI解析)**:
  * Geminiを活用したレシート画像解析（店舗名、商品名、金額、カテゴリ推測）。画像の向き自動補正機能。
  * **自動内税計算**: 消費税が含まれていない場合、システムが自動で「内税10%」項目を追加。
  * **チェックサム検証**: 合計金額と各明細の整合性チェックおよび自動再解析機能（リトライロジック付き）。
* **レシート手入力**:
  * キーボード主体で高速連続入力が可能なグリッド型UI。金額入力と同時に新規行が追加されるUIUX制御。
* **レシート修正**:
  * 過去履歴の検索、明細の個別修正（大・小分類変更）、レシート一括削除・ヘッダー（日付、店舗、支払方法）の一括更新機能。
  * 明細編集中は他のボタンやヘッダーを無効化するセーフティロック機能。

### 4.4. 相談・サポート
* **マニュアル**: アプリ内操作説明の閲覧。
* **ヘルプチャット**: アプリの機能や仕様についてAIがシステムプロンプトに従い即座に回答。
* **AI相談（専属FP）**:
  * ユーザーの家計データ全期間分を元にする、パーソナライズされたファイナンシャルプランナー機能。
  * User_Masterに登録された属性（MBTIやライフスタンス、ベースプロンプト）を付与し、人間味のある相談に対応。
  * **音声機能**: テキストだけでなく音声合成（読み上げ）や音声入力機能を搭載。

### 4.5. マスター設定
* **支払方法マスター**: 支払方法（クレカ、現金、電子マネー等）、クレカの締日・支払日の登録。
* **プロフィール設定**: AI相談をパーソナライズするための詳細な属性登録。

### 4.6. その他機能
* **データダウンロード機能**: `xlsxwriter`等を使用し、登録データをExcel (.xlsx) またはCSV形式でダウンロード。

## 5. カテゴリ定義
14の大分類（食材費、外食費、日用品、美容、衣類、家電、書籍、交通費、住居、娯楽、手数料、ペット用品、医療、園芸・植物）と、各項目に関連する小分類、および「割引・ポイント利用」「消費税（内税・外税）」「納税額」「支払い方法」「その他」という特別な分類が存在する。

## 6. 特記事項・エラーハンドリング
* **APIリトライ機構**: Google Sheets API上限エラー (429) や Gemini APIのタイムアウト・リソース枯渇等に対応するため、指数バックオフを用いた自動再試行ロジック (`safe_gspread_call`, `safe_gemini_call`) が実装されている。
* **モバイル対応レイアウト**: 入力フォーム等はCSSメディアクエリを用いて、スマートフォン閲覧時に最適なグリッドレイアウトへ強制変更されるよう最適化されている。
